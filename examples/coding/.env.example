# Provider API keys (see spec §7.2). Copy to `.env` and fill in.
# `model-catalog.json` uses the `deepseek` provider for catalog entries
# "fast" and "smart" (workflows/fix uses "smart").

DEEPSEEK_API_KEY=sk-...
