# DeepSeek API key for live LLM steps (see model-catalog.json).
# Copy to .env in this directory, or export in your shell.
DEEPSEEK_API_KEY=

# Required by project.json env whitelist:
ENGINEER_NAME=Ada Lovelace
SHIP_API_TOKEN=demo-token
