# Log — July 2026

Decision and milestone log. Do not record prompts or duplicate git history.

## 2026-07-06 — Documentation workflow

- Adopted split: `STATUS.md` (rewritten snapshot), `TASKS.md` (active queue), monthly log (decisions only).
- Added `.cursor/rules/project-docs.mdc` and `session-handoff` skill.
- Reason: keep agent context small; code + git remain source of truth for implementation.

## 2026-07-07 — Spec v1 pinned

- Turned `idea.md` into `spec.md` with concrete v1 scope.
- Key design decisions locked for v1:
  - Workflow project = directory of `.md` (workflows/tools) + JSON manifest; file path is the qualified name.
  - Workflow markdown = YAML frontmatter (typed signature) + prose + fenced ` ```step` JSON blocks. One declaration per file.
  - Type system: base scalars + `Json`, `FileRef`, `List<T>`, `Record<{...}>`, `WorkflowRef`/`ToolRef`. Full static type-check before any execution.
  - Control flow v1: sequential only. `if`/`foreach`/`par` deferred to v1.1.
  - Persistence: content-addressed step results (`step-key = hash(qname, step id, resolved args)`) + append-only `trace.jsonl`. Resume by skipping cached step keys. Chosen over pure event-sourced replay because it handles non-deterministic LLM steps cleanly.
  - Workspace sandbox: canonicalised root, path-traversal rejected, project dir read-only during exec.
  - `llm-simple` consumed as local `packages:` entry in `cabal.project`.
- Deferred to v2 (per idea.md stage 2): dynamic workflow synthesis by agents, trace introspection, skill extraction. Spec §13 lists these explicitly so v1 data model stays compatible.
- Open items still flagged in spec: markdown parser choice, exact frontmatter grammar, expression sub-language, CLI command name, run directory name.

## 2026-07-07 — Spec v1 revisions

- Replaced JSON step blocks with a small DSL: `bind <- qname(args)`, optional
  `@id`, `_ <-` for discard, explicit `return { ... }` blocks. Multi-line
  strings via `"""..."""`, string interpolation via `${...}`.
- Added `@self#<heading-slug>` expression to reference raw markdown content
  under a heading in the current file. Locked in for v1 (not deferred).
- Added ambient typed `Context` in scope for every step:
  `{ workspace, run, self, inputs, trace, env }`. Access via `${ctx.*}`.
  No implicit threading at call sites.
- Escape hatch chosen: `builtin/introspect` tool (not a `${ctx.raw}`
  expression) so usage is visible as a step in the workflow source.
- Step-key hashing now includes only a projection of `ctx` restricted to
  *stable* fields. Steps that reference volatile fields
  (`ctx.trace`, `ctx.run.started_at`) or call `builtin/introspect` are
  statically marked non-cacheable and always re-execute on resume.
  Rationale: their purpose is to observe live state; caching defeats them.
- Added `Secret<T>` type in v1. Auto-tag env vars matching
  `*_KEY|*_TOKEN|*_SECRET|*_PASSWORD`. Redacted in `trace.jsonl` as
  `<secret:$name>`. Forbidden from plain-string interpolation.
- `project.json` gains an optional `env` whitelist; nothing else from the
  process env is visible via `ctx.env`.
- Type checker factored as pure `Project -> Either [TypeError] TypedProject`
  so v1.1 dynamic-workflow evaluation can reuse it at runtime.
- Acceptance criteria expanded to A1–A9 to cover the new behaviour.

## 2026-07-07 — Grammars pinned

- Added §3.4 with a full EBNF for the step DSL and expression sub-language,
  plus the frontmatter `TypeExpr` string grammar. Reserved keywords locked
  (`return`, `true`, `false`, `null`, `_`); comment syntax `--` to EOL;
  slug rules for `@self#`; whitespace/newline handling inside vs outside
  brackets.
- Rewrote §8.3 as a stable trace event schema: `RunStart`, `StepStart`,
  `StepEnd`, `LlmCall`, `FileIo`, `Error`, `RunEnd`, all with common
  fields `tag`/`seq`/`at` and step-correlation `qname`/`step_id`. Added
  ordering + correlation invariants (matched start/end, nested
  sub-workflow calls, monotonic gap-free `seq`, uniform cached-step
  shape) and a redaction rule. Chosen now (rather than at M5) because
  `ctx.trace` exposes this ADT to workflows via §5.2, so agents will
  pattern-match on it; changing the shape later is costly.
- TASKS 2.3–2.6 and 5.4 updated to reference the pinned grammar/schema.

## 2026-07-07 — Final pre-M1 decisions

Closed out the last `[open]` markers in the spec so M1 has a clean target.

- **Binary name: `hwfi`.** Run directory: `.hwfi/` inside the workspace.
  Workspace lock: `.hwfi/lock`.
- **Test framework: `hspec`.** Markdown parser: `commonmark-hs`.
- **Shared types in v1.** Declared as markdown files under `types/` with
  frontmatter (`kind: type-alias`, `name`, `definition: <TypeExpr>`).
  Aliases resolve by qname; cycles rejected. Chose markdown-with-frontmatter
  over JSON to keep the "everything is markdown" story consistent.
- **API keys via `llm-simple`'s `.env` flow.** Read `../llm-simple/src/LLM/Load/LoadGateways.hs`
  to confirm behaviour:
  - `Configuration.Dotenv.loadFile defaultConfig` reads `<cwd>/.env` at
    gateway load, silently swallows missing file.
  - Provider keys pulled from process env: `OPENAI_API_KEY`,
    `CLAUDE_API_KEY` (**not** `ANTHROPIC_API_KEY`), `GEMINI_API_KEY`,
    `DEEPSEEK_API_KEY`. `ollama` gateway is always registered.
  - Keys never surface as Haskell values; held inside gateway closures.
  - Decision: `hwfi` sets cwd to the project directory at startup, so
    `.env` lives at `<project>/.env`. API keys never flow through
    `ctx.env` (whitelist governs only what workflows can read). Spec §7.2
    documents this asymmetry explicitly.
- **Model catalog.** `builtin/llm-*` `model` argument names a
  `modelConfigName` in the effective catalog (not a raw provider model
  id). Engine ships a default catalog; project-local
  `<project>/model-catalog.json` fully replaces it in v1. Merging
  deferred to v6.8.
- **CLI structured inputs.** `--input k=v` (string), `--input k=@file.json`
  (JSON from file), `--input-json <file>` (whole record). Composition
  rules pinned in §9.
- **Error format.** `file:line:col: <message>` with source excerpt and
  caret. Applies to parse and type errors; runtime errors also carry
  `(qname, step_id)` matching the trace. §9.1.
- Added acceptance criteria A10 (type-alias resolution + cycle
  rejection) and A11 (unknown model name lists available options).

## 2026-07-07 — Revised: skip `LLM.Load`, own key store

Reconsidered the `llm-simple` `Load` decision after user push-back.
`LLM.Load.loadGateways` combines three concerns we want separated:
`.env` discovery via cwd, key lookup from hardcoded env-var names, and
gateway construction. Using it forced us into:

- Chdir at startup (surprising, affects unrelated CLI path resolution).
- `.env` pinned to project dir (couples secrets to source tree).
- Partial `error` + silent `catch SomeException` in the wrappers.
- Inconsistency with our own `Secret<T>` discipline (keys enter as
  opaque closures, never as typed secrets).

**New design (spec §7.2, §7.3 rewritten):**

- `hwfi` uses `LLM.Providers.*` gateway constructors and
  `LLM.Load.ModelCatalog.loadModelCatalog` directly. Skips
  `LLM.Load.loadGateways` and the `*OrThrow` model loaders.
- Own `KeyStore :: Map ProviderName (Secret Text)`, populated by
  merging (highest first) `--env-file`, `<project>/.env`,
  process environment. Uses `Configuration.Dotenv.parseFile` (no
  process-env injection). No chdir.
- Every project **must** ship its own `model-catalog.json`. No engine
  default. Simpler, more explicit, avoids "our default changed"
  regressions. Dropped backlog task 6.8 (catalog merging); replaced
  with 6.8 for a user-level `$XDG_CONFIG_HOME/hwfi/.env` fallback.
- Provider–key linkage validated at startup against catalog contents.
  Missing key fails immediately with a message naming the offending
  model and provider (new A12).
- Added `--env-file` CLI flag (§9). Removed the cwd/chdir language and
  the "engine default catalog" language from §7.
- TASKS 1.6 rewritten as `Hwfi.Runtime.KeyStore`; 1.7 rewritten to
  require project-local catalog; 4.5 rewritten as
  `Hwfi.Runtime.Gateways` on top of `LLM.Providers.*`.

## 2026-07-07 — Spec review fixes (pre-M1 hardening)

Addressed a review (temporary `issues.md`) before starting M1.

- **Cache invalidation on code edits (critical).** Step-key now includes
  a `callee-fingerprint`: a Merkle `fingerprint(d)` over the acyclic
  direct call graph (normalized AST + sorted callee fingerprints).
  Editing a called tool/sub-workflow (or anything transitive) now
  correctly busts dependent cached steps across abort/resume.
  `WorkflowRef`/`ToolRef` args contribute the referenced fingerprint,
  not just the qname. Spec §8.1; A13.
- **Missing env vars.** Decided **strict presence** (§5.7): an absent
  whitelisted `env` var aborts startup before any step. Keeps
  `ctx.env.X : String` total; avoids introducing `Optional<T>` in v1
  (deferred, §13). A14.
- **`TypeExpr` alias reference (bug).** Added `| QName` to the grammar
  and documented alias resolution + the deliberate `:` (types) vs `=`
  (values) split. §3.4.
- **Multi-turn chat.** Added `builtin/llm-chat` with a `messages`
  history (message-based `GenRequest`). Aligns with the agentic stage-2
  goal. §6; A16.
- **Interpolation of complex types.** Defined bare-ref vs in-string
  positions (§3.2.1) and a total rendering table (structured → compact
  JSON; `Bytes`/`Secret` rejected at check time). No runtime type errors
  from interpolation.
- **`ctx.trace` on resume.** Removed the earlier synthetic
  `StepStart/StepEnd(cached=true)` events (they made observed history
  depend on caching). New model: cached steps emit no new events; their
  original detailed events persist in `trace.jsonl`; `ctx.trace` is the
  full parse of the file (§8.3.5). Added a `Resumed` marker; `RunEnd`
  now only `completed|aborted` and appears once at logical-run end.
  Reworked ordering invariants for multi-attempt runs. A15.
- **Eval errors.** Added `eval` error kind for list OOB / missing `Json`
  fields / bad interpolation targets (§8.3.2). Documented that field
  access on opaque `Json` and list indexing are not statically checked
  (§5.6.7).
- **Return rule.** Tightened §5.6.5: explicit `return` required when
  `outputs` is non-empty, unless the last step's result type is
  structurally equal to `outputs`.
- TASKS updated: added 2.6b, 3.9 (interpolation/return), 3.10
  (fingerprinting), 4.6 (evaluator + eval errors), 5.5 (`ctx.trace`
  reconstruction); renumbered following items. Added A13–A16.

## 2026-07-07 — M1 project skeleton complete

- Milestone: cabal project builds; `cabal build`/`cabal test` green.
- Module layout chosen: `Hwfi.Cli`, `Hwfi.Compat`,
  `Hwfi.Project.Manifest`, `Hwfi.Runtime.{Secret,Provider,KeyStore,ModelCatalog}`.
- Decision: `ProviderName` is a closed sum type (openai/claude/gemini/
  deepseek/ollama) for exhaustiveness, rather than free-form `Text` as in
  `llm-simple`. Conversion to/from the catalog's `providerName` string via
  `providerText`/`parseProvider`.
- Decision: `Secret a` is a newtype with a redacting `Show`; source-name
  redaction for traces (§8.3.4) deferred to M5, kept separate.
- Decision: `Hwfi.Compat` centralises all `llm-simple` imports so runtime
  modules stay decoupled from its internal module layout; also the single
  place proving the dependency wiring compiles.
- `KeyStore` degrades a malformed/unreadable optional `.env` to empty
  rather than aborting; only a key required by the catalog fails startup
  (validation lives in `ModelCatalog.validateProviderKeys`, wired into
  `run` in M4). `CatalogMissingKey` message matches spec §7.3 verbatim.
- Enabled `OverloadedRecordDot` (required to read `ModelCatalogItem`
  fields, since `llm-simple` compiles with `NoFieldSelectors`) and
  `DuplicateRecordFields` (shared field names across CLI option records).
- Note for M2: `commonmark-hs` not yet in local cabal cache.

## 2026-07-07 — M2 parsing and AST complete

- Milestone: full project parse to AST; `cabal build`/`cabal test` green
  (41 examples). New deps: `commonmark`, `megaparsec`, `parser-combinators`,
  `yaml`, `scientific`, `parsec`.
- Decision: `Hwfi.Parse.Markdown` defines its own minimal `IsInline`
  (`Inlines` = plain text) and `IsBlock` (`Blocks` = headings + fenced
  `step` blocks only) commonmark targets, with `Rangeable` capturing
  source ranges. Everything else collapses to `mempty`. This keeps only
  what the engine needs and yields absolute source positions.
- Decision: frontmatter is /blanked/ (replaced with empty lines), not
  removed, before commonmark parsing, so body positions equal file
  positions with no offset arithmetic. Original lines are kept for
  verbatim `@self#slug` slicing.
- Decision: two lexer space consumers — `sc` (horizontal only) for
  statement-level tokens so newlines terminate statements, `scn`
  (newline-crossing + `--` comments) inside `()`/`[]`/`{}`. Step blocks
  parsed via `runParserAt` with the block's absolute start position.
- Decision: `${x}` as a whole expression → `ERef`; `${x}` inside a string
  → `SInterp` part (preserves §3.2.1 bare-vs-interpolated distinction for
  the checker). Numbers split Int/Double by presence of fraction/exponent.
- Clarification (YAML reality): record type strings contain `:` and must
  be quoted in frontmatter (`"Record<{ role: String }>"`). Spec examples
  show them unquoted; real files must quote. Consider updating §2.1/§3.4
  examples.
- Decision: declaration classification is by explicit frontmatter `kind`
  else by top directory (`workflows`/`tools`/`types`). `DeclPrompt`
  (kind: prompt) is representable though v1 has no cross-file prompt ref.
  `Tool` is a distinct type from `Workflow` but structurally identical in
  v1 (shared `Signature`/`Section`).
- Deferred to M3 (checker), not parser: no-shadowing rule (§3.4),
  import/alias resolution, name-uniqueness beyond file paths.

## 2026-07-07 — M3 type checker complete

- Milestone: `hwfi check` type-checks a whole project into a
  `TypedProject` (resolved signatures, per-step cacheability, Merkle
  fingerprints). 71 tests green across unit specs and expected-error
  fixtures (`test/fixtures/check/`).
- Decision: split the type world in two — the surface `TypeExpr` (AST,
  unresolved, alias refs allowed) vs `Hwfi.Type.Type` (resolved, used by
  the checker). Aliases are expanded during resolution; cycles are an
  error (A10).
- Decision: assignability is `structEq` plus one subtyping rule — a
  `String` is accepted where a `FileRef` is expected — so literal path
  strings can feed file-taking parameters without a cast. Records compare
  structurally (field order irrelevant); lists/secrets are congruent.
- Decision: declaration `Fingerprint` is a Merkle hash (crypton SHA256)
  over the canonical AST encoding plus the sorted callee fingerprints,
  taken over the acyclic direct call graph. Builtins are leaves with a
  fixed identity; bare first-class refs are not static callees.
- Blocker resolved (root cause): `computeFingerprints` hung on any
  multi-decl project. Cause: `Fingerprint` is a `newtype`, so forcing a
  value to WHNF forces its hash and therefore its callee lookups into the
  self-referential `fps` map; `Data.Map.Strict.mapWithKey` forces every
  value during construction, demanding a callee's fingerprint before
  `fps` exists → loop. Fix: build `fps` with `Data.Map.Lazy.mapWithKey`
  (tie-the-knot), leaving lookups on the shared map type unchanged.
- Decision: checker is pure (`Project -> Either [TypeError] TypedProject`),
  keeping IO in the CLI only — required for the deferred v1.1 dynamic
  workflow evaluation.

## 2026-07-07 — M4 runtime and built-in tools complete

- Milestone: `hwfi run` executes a checked project end-to-end. New modules
  `Hwfi.Runtime.{Value,Error,Trace,Workspace,Gateways,Context,Eval,Builtins,
  Executor}`. 102 tests green (was 71); new deps `time`, `random`, `vector`,
  `unliftio`. `examples/summarise/` added.
- Decision: a dedicated `RValue` runtime type, richer than aeson `Value`,
  so the executor keeps the distinctions JSON conflates — `FileRef` vs
  `String`, `Secret<_>` (redacted, carries source name) vs its payload, and
  first-class `ToolRef`/`WorkflowRef` values vs a qname string. The checker's
  static types tell the executor which constructor to build.
- Decision: interpolation of structured values and step-key hashing share
  one canonical JSON encoder (sorted object keys, compact) in
  `Hwfi.Runtime.Value`, so §3.2.1 rendering and §8.1 hashing can never drift.
- Decision: the trace event schema (§8.3) and an in-memory `Tracer` were
  built now, in M4, not deferred to M5 — `ctx.trace` and `builtin/introspect`
  both need live events. M5 layers the `trace.jsonl` writer + resume
  reconstruction on the same `emit` seam rather than redefining the shape.
- Decision: workspace traversal guard is *lexical* (collapse `.`/`..`, reject
  escapes), not `canonicalizePath`-based, so a symlink created mid-run can
  never redirect a `FileRef` outside the root (A5). Root is canonicalised
  once at startup.
- Decision: gateways built from `LLM.Providers.*` + our typed `KeyStore`
  (never `LLM.Load.loadGateways`, per §7.2); `ollama` is always present
  (no key). `ModelConfig` assembly mirrors `LLM.Load.LoadModels` but sources
  keys from the `KeyStore`. Unknown `model` args fail listing available
  names (A11).
- Decision: `ctx` is rebuilt per step because `self.step_id` and `trace`
  vary; the rest comes from an immutable `RunInfo`. `ctx.inputs` is always
  the *root* inputs (even inside sub-workflows), while the `inputs` binding
  is the current declaration's own inputs.
- Decision: errors nest — every failing step in a call chain emits its own
  `error` event tagged with its `(qname, step_id)`, so each `StepStart` has
  exactly one terminal (§8.3.3 invariant 3). The returned `RuntimeError`
  keeps the deepest step location for the CLI message.
- Deferred to M5 (as planned): `.hwfi/runs/<id>/`, `run.json`, `trace.jsonl`
  persistence, step-key caching + resume, workspace lock, `hwfi show`. The
  `run-start` `project_hash` is a fingerprint stand-in until M5's project-dir
  content hash.
- Note: `builtin/llm-chat` maps `tool`-role messages to a user turn for now;
  full `ToolResult` round-tripping (call ids) is a stage-2 concern.

## 2026-07-07 — M5 persistence, tracing, resume complete

- Milestone: `hwfi run` persists a run under `<workspace>/.hwfi/runs/<id>/`
  (`run.json`, `steps/<step-key>.json`, append-only `trace.jsonl`);
  `hwfi resume` re-executes skipping cached cacheable steps; `hwfi show`
  pretty-prints. 128 tests (was 102); A4/A7/A13/A15 covered.
- Decision: the `Tracer` gained an optional file sink — `emit` appends the
  event's JSON line and `hFlush`es, so a killed process leaves a durable
  prefix. `eventFromJson` is the exact inverse of `eventToJson`, so resume
  reconstructs `ctx.trace` and continues `seq` from the persisted tail, and
  `hwfi show` decodes the same shape. Same ADT, one seam.
- Decision: step results and root inputs are persisted as **actual**
  (non-redacted) values (cache/`run.json`), because resume must re-evaluate
  with real values; only `trace.jsonl` redacts (§8.3.4, A8). A cached result
  is reconstructed to a typed `RValue` via the step's static result type
  (new `TypedStep.tsResultType`), preserving `FileRef`/`Secret` distinctions
  JSON conflates. The step-key hashes actual (not redacted) secret values so
  distinct secrets get distinct keys without leaking (one-way hash).
- Decision: the cache is consulted **only on resume** but written on every
  attempt. Cacheability is a per-call-site static property: a cacheable step
  that calls a sub-workflow is skipped wholesale on a cache hit even if the
  callee contains non-cacheable steps (its result was persisted). Editing a
  callee changes its Merkle fingerprint → the caller's step-key → recompute
  (A13); a first-class `Ref` argument contributes its target's fingerprint,
  not its qname (§8.1).
- Decision: `run.json` records `project_dir` so `hwfi resume <ws> <id>` can
  re-parse and re-check the project (needed for A13) without re-supplying the
  path. `project_hash` is now a real content hash over all declaration
  fingerprints (replacing M4's entrypoint-fingerprint stand-in).
- Decision: the workspace lock is an advisory OS `flock` on
  `<ws>/.hwfi/lock`; a second *in-process* open (GHC's single-writer handle
  lock throws `ResourceBusy`) is caught and reported as "locked" rather than
  crashing. The trace reader reads strictly to avoid a lazy read handle
  clashing with the resumed attempt's append handle.

## 2026-07-07 — `examples/research` (feature-showcase example)

- Added a second, deliberately complex example under `examples/research/`
  exercising nearly every v1 feature: multi-workflow orchestration + sub-workflow
  nesting, all LLM builtins (`llm-generate`/`llm-chat`/`llm-gen-object`),
  `list-dir`/`read-file`/`write-file`, `introspect`, nested type aliases
  (`chat-log` → `message`) and a record alias (`doc-brief`), `@self#` prompts,
  `ctx.env` string + secret (`RESEARCH_API_TOKEN` redacted, A8), `ctx.trace`/
  `ctx.run.id` (non-cacheable audit, A7), `Json` CLI input, multi-line strings,
  record/list literals. `hwfi check` clean; ran end-to-end against local Ollama
  (`fast`=llama3.2, `smart`=mistral) producing `digest.md` + `audit/`.
- Finding (not a code change): first-class `ToolRef`/`WorkflowRef` values can be
  passed as arguments but **cannot be invoked** — a bare call target only
  resolves against top-level roots (`inputs`/`ctx`/prior binds) and every step
  bind is a record, so a ref never becomes a callable name. Documented as a known
  limitation in the example's README rather than worked around.

## 2026-07-07 — M6 LLM tool-use complete

- Milestone: `builtin/llm-agent` (free-text) and `builtin/llm-agent-object`
  (typed `submit`) drive an agentic tool-use loop. New modules
  `Hwfi.Runtime.{Schema,Agent}`; `Gateways.modelCatalogFingerprint`; agent
  trace events; bespoke agent type-check. 152 tests (was 128); A17–A21.
- Decision (scope): implemented a **dedicated agent-loop** reified state
  machine over `RValue` (a round/tool-call machine, modelled on
  `../llm-workflow` but without `unsafeCoerce` since hwfi values are already
  dynamically typed), rather than refactoring the whole executor into a CEK
  machine as `tool-use.md` sketches for M7. Rationale: M6 has one consumer;
  the general control-flow unification is justified only once M7 (`if`/
  `foreach`/`par`) provides the second. Flagged so M7 revisits it.
- Decision (agent eligibility, §6.1.1/§6.1.5): a declaration may be advertised
  as a tool only if `typeToSchema` succeeds for every input (rejecting
  `Secret<_>`, `ToolRef`/`WorkflowRef`, `Bytes`) and it does not transitively
  reach `builtin/introspect`. Introspect-reachability is a cycle-safe graph
  closure (`Hwfi.Check.reachesIntrospect`) threaded into `CheckCtx`, so the
  check is robust even with import cycles. Agent builtins take a heterogeneous
  `tools` list with no v1 union type, so they bypass the generic `checkArgs`
  path entirely (`checkAgentCall`/`checkToolElem`).
- Decision (caching, §8.2.1): the agent step is a non-cacheable black box, but
  each **model call** (sub-keyed by agent step-key + round + model-catalog
  fingerprint + advertised-tools fingerprint + canonical messages) and each
  **tool call** (sub-keyed by step-key + round + call index + callee
  fingerprint + canonical args) is content-addressed in `RunStore`. Resume
  consults these sub-caches and replays the model's prior choices and tool
  results without re-paying the LLM or re-running side effects. Verified by an
  end-to-end test that resumes against a gateway/dispatch that fail if touched.
- Decision (tool-result caching): cache the **redacted** JSON of a tool result,
  not the actual `RValue`. Reconstruction shows the model the same redacted
  content it originally saw, and no secret ever reaches the intra-step cache.
  The rare case of an agent tool returning a secret is an accepted limitation.
- Decision (trace ordering): `agent-round-start`/`-round-end` are emitted
  lazily via an `IORef` guard, so a resumed round whose model and tool calls
  are all cache hits emits no new events (§8.3.3.7); the marker is written on
  the first real event of the round. A model tool call is bracketed by a nested
  `step-start`/`step-end` (or `error`) so the callee's events nest under the
  agent step and every start has exactly one terminal.
- Decision (error classes, §6.1.4): malformed/ill-typed tool arguments, unknown
  tools, callee-surfaced errors, mixed-`submit` rounds, and `submit` schema
  failures are **recoverable** — fed back to the model as a tool result so it
  can retry. Only `KInternal` dispatch failures and `max_rounds` exhaustion are
  fatal and abort the step.
- Testing: the agent loop is exercised via a fake `LLMGateway` scripted by
  conversation round (counting `ToolTurn`s), wired into a `ModelConfig` with no
  retries/timeout. `llm-simple` added as a test dependency. Covers free-text
  termination, unknown-tool feedback, `max_rounds` fatality, object/`submit`,
  mixed-submit rejection then retry, schema-invalid submit retry, and
  cached-resume replay (A17–A21).
